export const metadata = {
  title: "Blog | Harjot Singh",
  description: "Blogs by Harjot Singh",
};

export default function BlogLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
